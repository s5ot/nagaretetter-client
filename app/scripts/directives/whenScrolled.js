/*
angular.module('jwaveyarouClientApp', [])
.directive('whenScrolled',  function() {
    return function(scope, eim, attr) {
        var raw = elem[0];
        elm.bind('scroll', function() {
            console.log(elm);
            if (raw.scrollTop + raw.offsetHeight >= raw.scrollHeight) {
                scope.$apply(attr.whenScrolled);
            }
        });
    };
});
*/
