general:
  branches:
    only:
      - master # list of branches to build
      - /angular-1-4/ # or regexes
dependencies:
  override:
    - gem install sass compass:
        timeout: 600

test:
  pre:
    - npm install -g karma
    - npm install -g bower
    - bower install --config.directory=./app/bower_components
