general:
  branches:
    only:
      - master # list of branches to build
      - /angular-1-4/ # or regexes
dependencies:
  override:
    - gem install sass compass:
        timeout: 600
  pre:
    - npm install -g bower
    - npm install
    - bower install
